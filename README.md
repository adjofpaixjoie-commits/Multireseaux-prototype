# Prototype — Publication multi-réseaux (Telegram + X)

Prototype minimal permettant de publier un fichier en un clic sur Telegram et X (Twitter), avec estimation du coût par publication.

## Installation

```bash
npm install
cp .env.example .env
